# Trading Scanner — Week 1
# Python 3.11+ required (pyenv local 3.11.9 in ~/trading/)
#
# Install: pip install -r requirements.txt

# Market data — volume scanning, TA, news (direct Python import, no MCP server needed)
tradingview-mcp-server>=0.7.0

# Options chains + earnings proximity
yfinance>=0.2.40

# Data manipulation
pandas>=2.0.0

# LLM (Week 3 — orchestrate.py)
anthropic>=0.25.0

# HTTP
requests>=2.31.0

# Testing
pytest>=8.0.0
