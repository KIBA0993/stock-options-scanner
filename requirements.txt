# Trading Scanner — Week 1
# Python 3.11+ required (pyenv local 3.11.9 in ~/trading/)
#
# Install: pip install -r requirements.txt

# Market data — volume scanning, TA (scanner.py uses tradingview-screener directly)
tradingview-screener>=3.2.0

# Options chains + earnings proximity
yfinance>=0.2.40

# Data manipulation
pandas>=2.0.0

# LLM (orchestrate.py, distill.py, vertex_llm.py)
anthropic>=0.25.0
openai>=1.30.0
google-auth>=2.29.0

# HTTP
requests>=2.31.0

# Paper execution (P2-2 — optional; only needed when alpaca.enabled=true)
# alpaca-py>=0.30.0

# Testing
pytest>=8.0.0
